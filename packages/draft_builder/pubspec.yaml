name: draft_builder
description: The build runner for the draft package.
version: 0.0.1
repository: https://github.com/josiahsrc/draft

environment:
  sdk: ^3.7.0

dependencies:
  analyzer: ^6.9.0
  build: ^2.3.1
  source_gen: ^1.0.0
  draft:
    path: ../draft

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  build_test: ^2.2.3
  build_runner: ^2.3.3
